<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.MuseumMapper">

    <resultMap id="BaseResultMap" type="com.example.entity.Museum">
        <id property="id" column="id"/>
        <result property="museumCode" column="museum_code"/>
        <result property="museumName" column="museum_name"/>
        <result property="museumType" column="museum_type"/>
        <result property="province" column="province"/>
        <result property="city" column="city"/>
        <result property="address" column="address"/>
        <result property="contactPerson" column="contact_person"/>
        <result property="contactPhone" column="contact_phone"/>
        <result property="contactEmail" column="contact_email"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="selectByPage" resultMap="BaseResultMap">
        SELECT * FROM museum WHERE 1=1
        <if test="museumName != null and museumName != ''">
            AND museum_name LIKE CONCAT('%', #{museumName}, '%')
        </if>
        <if test="city != null and city != ''">
            AND city = #{city}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY id ASC
    </select>

    <select id="selectAllActive" resultMap="BaseResultMap">
        SELECT * FROM museum WHERE status = 1 ORDER BY museum_code
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM museum WHERE id = #{id}
    </select>

    <select id="selectByCode" resultMap="BaseResultMap">
        SELECT * FROM museum WHERE museum_code = #{museumCode}
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT * FROM museum WHERE museum_name = #{museumName}
    </select>

    <insert id="insert" parameterType="com.example.entity.Museum"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO museum (museum_code, museum_name, museum_type, province, city, address,
        contact_person, contact_phone, contact_email, description, status)
        VALUES (#{museumCode}, #{museumName}, #{museumType}, #{province}, #{city}, #{address},
        #{contactPerson}, #{contactPhone}, #{contactEmail}, #{description}, #{status})
    </insert>

    <update id="update" parameterType="com.example.entity.Museum">
        UPDATE museum SET
            museum_name = #{museumName},
            museum_type = #{museumType},
            province = #{province},
            city = #{city},
            address = #{address},
            contact_person = #{contactPerson},
            contact_phone = #{contactPhone},
            contact_email = #{contactEmail},
            description = #{description},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM museum WHERE id = #{id}
    </delete>

</mapper>
