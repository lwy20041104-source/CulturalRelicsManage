<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AiChatMessageMapper">

    <resultMap id="BaseResultMap" type="com.example.entity.AiChatMessage">
        <id property="id" column="id"/>
        <result property="sessionId" column="session_id"/>
        <result property="userId" column="user_id"/>
        <result property="messageType" column="message_type"/>
        <result property="content" column="content"/>
        <result property="queryKeyword" column="query_keyword"/>
        <result property="resultCount" column="result_count"/>
        <result property="hasExternalResult" column="has_external_result"/>
        <result property="relicIds" column="relic_ids"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.example.entity.AiChatMessage"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_message (session_id, user_id, message_type, content, query_keyword, result_count, has_external_result, relic_ids)
        VALUES (#{sessionId}, #{userId}, #{messageType}, #{content}, #{queryKeyword}, #{resultCount}, #{hasExternalResult}, #{relicIds})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM ai_chat_message WHERE id = #{id}
    </select>

    <select id="selectBySessionId" resultMap="BaseResultMap">
        SELECT * FROM ai_chat_message WHERE session_id = #{sessionId} ORDER BY create_time ASC
    </select>

    <select id="selectRecentByUserId" resultMap="BaseResultMap">
        SELECT * FROM ai_chat_message WHERE user_id = #{userId} ORDER BY create_time DESC LIMIT #{limit}
    </select>

    <delete id="deleteBySessionId">
        DELETE FROM ai_chat_message WHERE session_id = #{sessionId}
    </delete>

    <select id="countBySessionId" resultType="int">
        SELECT COUNT(*) FROM ai_chat_message WHERE session_id = #{sessionId}
    </select>

</mapper>
