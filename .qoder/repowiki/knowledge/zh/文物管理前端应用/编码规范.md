- API 模块采用单文件对应业务领域（如 `relics.js`, `users.js`），并统一引用 `request.js` 实例发起请求。
- 视图组件优先使用 `<script setup>` 语法糖，并结合 Composition API 组织逻辑。
- 路由配置中通过 `meta.perm` 字段定义权限标识，并在布局组件中通过 `hasPerm` 函数控制菜单项显示。
- 全局状态（如用户信息、权限列表）优先从 `sessionStorage` 读取，而非全部依赖 Pinia Store。